# This is a Declarative Automation Bundle definition for feature_1.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: feature_1
  uuid: b7cef418-9565-4985-aa25-7465d0bb6584

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-0e4cf451-9f30.cloud.databricks.com
    variables:
      catalog: dev_catalog
      schema: rnd
  prod:
    mode: production
    workspace:
      host: https://dbc-0e4cf451-9f30.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/sameermd1857978@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/sameermd1857978@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dev_catalog
      schema: prod
    permissions:
      - user_name: sameermd1857978@gmail.com
        level: CAN_MANAGE
